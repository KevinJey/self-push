#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        int left,right;
        map<int,int> mp; 
        for(auto it: nums)
        left = 0;
        right = nums.size()-1;
        while(left < right){
            
        }
    }
};